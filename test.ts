// Import hàm question từ thư viện readline-sync
import { question } from 'readline-sync';


let con = function (name: string, ...array: number[]): void {
    console.log('sadasd', array)
}

con('Tung', 3, 4, 8, 9);